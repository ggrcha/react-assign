import React from 'react';

const userOutput = (props) => { return (
        <div>
            <p>{props.userName}</p>
            <p>Outro texto randomico</p>
        </div>
    )
}

export default userOutput